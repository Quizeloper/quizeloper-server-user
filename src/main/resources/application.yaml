spring:
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    generate-ddl: false
    show-sql: false
    properties:
      hibernate:
        format_sql: true

spring.sql.init.platform: mysql

spring.profiles.include: secret